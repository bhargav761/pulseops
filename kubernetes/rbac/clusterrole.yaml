apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRole
metadata:
  name: pulseops-readonly

rules:
- apiGroups: [""]
  resources:
    - namespaces
    - nodes
    - pods
  verbs:
    - get
    - list
    - watch