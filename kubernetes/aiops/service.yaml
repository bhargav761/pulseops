apiVersion: v1
kind: Service

metadata:
  name: pulseops-ai
  namespace: pulseops

  labels:
    app: pulseops-ai

spec:

  type: ClusterIP

  selector:
    app: pulseops-ai

  ports:

    - name: http
      protocol: TCP
      port: 8001
      targetPort: 8001